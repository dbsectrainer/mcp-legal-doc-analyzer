import type { Template } from "../types.js";
/**
 * list_templates tool handler.
 * Returns all available template names and descriptions.
 */
export declare function toolListTemplates(templates: Map<string, Template>): string;
